# credentials — NEVER commit
keys.json

# reference solutions (excluded from the distributed repo)
solutions/

# generated data / training artifacts
dataset/
checkpoint/
results/
hardware/runs/
# slurm_logs/ 폴더 자체는 동기화하되 내용물(로그)만 무시
slurm_logs/*
!slurm_logs/.gitkeep
coupling_*.json

# python
__pycache__/
*.pyc
*.egg-info/

# editors
.vscode/
.idea/

# hardware runtime state (per-machine)
hardware/pending_jobs.json
hardware/collect_failed.log
hardware/placement_*.json
